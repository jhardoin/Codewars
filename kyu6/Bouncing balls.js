function bouncingBall(h,  bounce,  window) {
    if(h <= 0 || bounce <= 0 || bounce >= 1 || window >= h) return -1
    let count = 0
    while(h > window){
      count++
      h = h * bounce
      if (h > window) count++
    }
    return count
  }
  

  function bouncingBall(h,  bounce,  window) {
    if(h <= 0 || bounce <= 0 || bounce >= 1 || window >= h) return -1
    let count = 0
    while(h > window){
      count++
      h = h * bounce
      if (h > window) count++
    }
    return count
  }
  

  function bouncingBall(h,  bounce,  window) {
    if(h <= 0 || bounce <= 0 || bounce >= 1 || window >= h) return -1
    let count = 0
    while(h > window){
      count++
      h = h * bounce
      if(h > window) count++
    }
    return count
}

function bouncingBall(h,  bounce,  window) {
  if(h <= 0 || bounce <= 0 || bounce >= 1 || window >= h) return -1
  let count = 0
  while(h > window){
    count++
    h = h * bounce
    if(h > window)count++
  }
  return count
}

function bouncingBall(h,  bounce,  window) {
  if(h <= window || bounce <= 0 || bounce >= 1 || window >= h) return -1
  let count = 0
  while(h > window){
    count++
    h = h * bounce
    if(h > window) count++
  }
  return count
}

function bouncingBall(h,  bounce,  window) {
  if(h <= 0 || bounce <= 0 || bounce >= 1 || window >= h) return -1
  let count = 0
  while(h > window){
    count++
    h = h * bounce
    if(h > window) count++
  }
  return count
}

function bouncingBall(h,  bounce,  window) {
  if(h <= 0 || bounce <= 0 || bounce >= 1 || window >= h) return -1
  let count = 0
  while(h > window){
    count++
    h = h * bounce
    if(h > window) count++
  }
  return count
}

function bouncingBall(h,  bounce,  window) {
  if(window >= h || bounce <= 0 || bounce >= 1) return -1
  let count = 0
  while(h > window){
    count++
    h = h * bounce
    if(h > window) count++
  }
  return count
}

function bouncingBall(h,  bounce,  window) {
  if(h <= 0 || bounce <= 0 || bounce >= 1 || window >= h) return -1
  let count = 0
  while(window < h){
    count++
    h = h * bounce
    if(window < h)count++
  }
  return count
}

function bouncingBall(h,  bounce,  window) {
  if(h <= 0 || bounce <= 0 || bounce >= 1 || window >= h) return -1
  let count = 0
  while(h > window){
    count++
    h = h * bounce
    if(h > window) count++
  }
  return count
}