function inArray(array1, array2) {
    return array1.filter(e => array2.some(el => el.includes(e))).sort()
  }
  

  function inArray(array1,array2){
    return array1.filter(e => array2.some(el => el.includes(e))).sort()
}

function inArray(array1,array2){
  return array1.filter(e => array2.some(el => el.includes(e))).sort()
}

function inArray(array1,array2){
  return array1.filter(e => array2.some(el => el.includes(e))).sort()
}

function inArray(array1,array2){
  return array1.filter(e => array2.some(el => el.includes(e))).sort()
}