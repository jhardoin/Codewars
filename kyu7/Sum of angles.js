const angle = n => (n - 2) * 180;

const angle = n => n == 3 ? 180 : 180 + (n - 3) * 180

const angle = n => (n - 2) * 180;

function angle(n) {
    return (n - 2) * 180
  }

  function angle(n) {
    return (n - 2) * 180
}

function angle(n) {
  let degrees = 180
  for(let i = 3; i < n; i++){
    degrees += 180
  }
  return degrees
}

function angle(n) {
  return (n - 2) * 180
}

function angle(n) {
  return (n - 2) * 180
}

function angle(n) {
  return (n - 2) * 180
}

function angle(n) {
  return (n - 2) * 180
}