function mygcd(x,y){
    if(y == 0) return x
    else return mygcd(y, x % y)
  }

  function mygcd(x,y){
    for(let i = x ; i > 0;i -= 1){
      if (x%i === 0 && y%i===0){
        return i
      }
    } 
  }

  function mygcd(x,y){
    for(let i = x ; i > 0;i -= 1){
      if (x%i === 0 && y%i===0){
        return i
      }
    } 
  }

  function mygcd(x,y){
    for(let i = x ; i > 0;i -= 1){
      if (x%i === 0 && y%i===0){
        return i
      }
    } 
  }