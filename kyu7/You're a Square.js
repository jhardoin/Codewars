let isSquare = (n) => {
    let root = Math.sqrt(n);
    if(root % 1 === Number()) {
      return true;
    }
    else {
      return false;
    }
  }

  var isSquare = n => Math.sqrt(n) % 1 == 0

  const isSquare = n =>  Math.sqrt(n) % 1 == 0