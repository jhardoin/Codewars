const minValue = v => Number(Array.from(new Set(v)).sort((a,b) => a - b).join(''))

const minValue = values => +[...new Set(values)].sort((a,b) => a - b).join('')

function minValue(values){
    return +[...new Set(values)].sort((a,b) => a - b).join('')
  }

  function minValue(values){
    return +[...new Set(values)].sort((a,b) => a - b).join('')
}

function minValue(values){
  return +[...new Set(values)].sort((a,b) => a - b).join('')
}

function minValue(values){
  return +[...new Set(values)].sort((a,b) => a - b).join('')
}