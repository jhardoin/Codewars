const triangular = n => (n < 1) ? 0 : (n * (n + 1)) / 2

function triangular( n ) {
    return n > 0 ? n * (n + 1) / 2 : 0;
  }

  function triangular( n ) {
    return n > 0 ? n * (n + 1) / 2 : 0;
  }

  function triangular( n ) {
    return n > 0 ? n * (n + 1) / 2 : 0;
  }