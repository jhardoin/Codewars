function productArray(numbers) {
    let totalProduct = numbers.reduce((acc, num) => acc * num, 1)
    return numbers.map(num => totalProduct / num)
  }

  function productArray(numbers) {
    let totalProduct = numbers.reduce((acc, num) => acc * num, 1)
    return numbers.map(num => totalProduct / num)
  }