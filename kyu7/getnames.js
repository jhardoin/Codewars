function getNames(data){
    return data.map(e => e.name)
  }

  function getNames(data){
    return data.map(e => e.name)
  }
  
  
  function getNames(data){
    return data.map(e => e.name)
  }

  function getNames(data){
    return data.map(e => e.name)
  }

  function getNames(data){
    return data.map(e => e.name)
  }

  function getNames(data){
    return data.map(e => e.name)
  }