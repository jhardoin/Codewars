function solve(s){
    return Math.max(...[...s].map(e => 'aeiou'.includes(e) ? e : ' ').join('').split(' ').map(e => e.length))
   }

   function solve(s){
    let arr = s.split('').map(e => 'aeiou'.includes(e) ? e : ' ').join('').split(' ')
    return Math.max(...arr.map(e => e.length))
  }

  function solve(s){
    let arr = s.split('').map(e => 'aeiou'.includes(e) ? e : ' ').join('').split(' ')
    return Math.max(...arr.map(e => e.length))
  }

  function solve(s){
    let arr = s.split('').map(e => 'aeiou'.includes(e) ? e : ' ').join('').split(' ')
    return Math.max(...arr.map(e => e.length))
  }

  function solve(s){
    let arr = s.split('').map(e => 'aeiou'.includes(e) ? e : ' ').join('').split(' ')
    return Math.max(...arr.map(e => e.length))
  }