function vowel2index(str) {
    return str.split('').map((e,i) => 'aeiouAEIOU'.includes(e) ? i + 1 : e).join('')
 }
 
 function vowel2index(str) {
    return str.split('').map((e,i) => 'aeiouAEIOU'.includes(e) ? i + 1 : e).join('')
 }
 

 function vowel2index(str) {
   return str.split('').map((e,i) => 'aeiouAEIOU'.includes(e) ? i + 1 : e).join('')
}


function vowel2index(str) {
   return str.split('').map((e,i) => 'aeiouAEIOU'.includes(e) ? i + 1 : e).join('')
}
