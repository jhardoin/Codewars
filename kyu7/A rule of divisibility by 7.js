function seven(m) {
    let count = 0
    
    while(m.toString().length > 2){
      m = (+m.toString().slice(0, -1)) - (2 * +m.toString().slice(-1))
      count++
    }
   
  return [m, count]
}

function seven(m) {
  let count = 0
  
  while(m.toString().length > 2){
    m = (+m.toString().slice(0, -1)) - (2 * +m.toString().slice(-1))
    count++
  }
 
return [m, count]
}

function seven(m) {
  let count = 0
  
  while(m.toString().length > 2){
    m = (+m.toString().slice(0, -1)) - (2 * +m.toString().slice(-1))
    count++
  }
 
return [m, count]
}