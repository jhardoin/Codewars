const spacify = str => str.split('').join(' ')

function spacify(str) {
    return [...str].join(' ')
  }

  function spacify(str) {
    return [...str].join(' ')
  }

  function spacify(str) {
    return [...str].join(' ')
  }

  function spacify(str) {
    return [...str].join(' ')
  }