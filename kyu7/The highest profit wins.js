const minMax = arr => [Math.min(...arr), Math.max(...arr)]

const minMax = arr => [Math.min(...arr), Math.max(...arr)];

function minMax(arr){
    return [Math.min(...arr), Math.max(...arr)]
  }

  function minMax(arr){
    return [Math.min(...arr),Math.max(...arr)]
  }