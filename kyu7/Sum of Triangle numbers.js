function sumTriangularNumbers(n) {
    if(n < 0) return 0
    return n * (n + 1) * (n + 2) / 6;
  }

  function sumTriangularNumbers(n) {
    return n < 0 ? 0 : n * (n + 1) * (n + 2) / 6;
  }

  function sumTriangularNumbers(n) {
    return n < 0 ? 0 : n * (n + 1) * (n + 2) / 6;
  }