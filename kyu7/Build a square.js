function generateShape(integer){
  return ('+'.repeat(integer) + '\n').repeat(integer).trim()
}

function generateShape(integer){
  return `${('+'.repeat(integer))}\n`.repeat(integer).trim()
}

function generateShape(n){
  return ("+".repeat(n)+"\n").repeat(n).trim()
}