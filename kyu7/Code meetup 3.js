function isRubyComing(list) {
    return list.some(e => e.language == 'Ruby')
  }

  function isRubyComing(list) {
    return list.some(e => e.language == 'Ruby')
  }

  function isRubyComing(list) {
    return list.some(e => e.language == 'Ruby')
  }

  function isRubyComing(list) {
    return list.some(e => e.language == 'Ruby')
  }