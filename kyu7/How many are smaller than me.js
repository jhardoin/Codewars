function smaller(nums) {
    return nums.map((e,i,a) => a.slice(i).filter(x => e > x).length)
}

function smaller(nums) {
    return nums.map((e,i,a) => a.slice(i).filter(x => e > x).length)
}

function smaller(nums) {
    return nums.map((e,i,a) => a.slice(i).filter(x => e > x).length)
}

function smaller(nums) {
    return nums.map((e,i,a) => a.slice(i).filter(x => e > x).length)
}

function smaller(nums) {
    return nums.map((e,i,a) => a.slice(i).filter(x => e > x).length)
}

function smaller(nums) {
    return nums.map((e,i,a) => a.slice(i).filter(x => e > x).length)
}

function smaller(nums) {
    return nums.map((e,i,a) => a.slice(i).filter(x => e > x).length)
}