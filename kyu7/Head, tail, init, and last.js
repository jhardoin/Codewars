const head = x => x[0]
const tail = x => x.slice(1)
const init = x => x.slice(0, x.length - 1)
const last = x => x[x.length-1]

let head = arr => arr[0];
let tail = arr => arr.slice(1);
let init = arr => arr.slice(0, arr.length-1);
let last = arr => arr[arr.length-1];