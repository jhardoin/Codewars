const smallEnough = (a, limit) => a.every(e => e <= limit)

const smallEnough = (a, limit) => a.every(e => e <= limit)

function smallEnough(a, limit){
    return a.every(e => e <= limit)
  }

  function smallEnough(a, limit){
    return a.every(e => e <= limit)
  }

  function smallEnough(a, limit){
    return a.every(e => e <= limit)
  }