function searchNames( logins ){
    return logins.filter(e => e[0].endsWith('_'))
  }

  function searchNames( logins ){
    return logins.filter(e => e[0].endsWith('_'))
  }

  
  function searchNames( logins ){
    return logins.filter(e => e[0].endsWith('_'))
  }

  function searchNames( logins ){
    return logins.filter(e => e[0].endsWith('_'))
  }

  function searchNames( logins ){
    return logins.filter(e => e[0].endsWith('_'))
  }