function getSumOfDigits(integer) {
    return [...integer.toString()].reduce((a,b) => +b + a, 0)
  }

  function getSumOfDigits(integer) {
    return [...integer.toString()].reduce((a,b) => +b + a, 0)
  }