function countRedBeads(n) {
    if(n < 2) return 0
    else return n * 2 - 2
  }

  function countRedBeads(n) {
    return n < 2 ? 0 : 2 * (n - 1)
  }

  function countRedBeads(n) {
    return n > 0 ? (n - 1) * 2 : 0
}