function catMouse(x){
    return x.slice(1,-1).length > 3 ? 'Escaped!' : 'Caught!'
  }

  function catMouse(x){
    return x.slice(1,-1).length > 3 ? 'Escaped!' : 'Caught!'
  }

  function catMouse(x){
    return x.slice(1,-1).length > 3 ? 'Escaped!' : 'Caught!'
  }

  function catMouse(x){
    return x.slice(1,-1).length > 3 ? 'Escaped!' : 'Caught!'
  }