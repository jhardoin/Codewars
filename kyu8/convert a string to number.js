const stringToNumber = function(str){
    return parseInt(str)
  }

  const stringToNumber = str => +str

  const stringToNumber = str => +str

  const stringToNumber = str => +str

  const stringToNumber = function(str){
    return +str
  }

  const stringToNumber = function(str){
    return +str
  }

  const stringToNumber = function(str){
    return +str
}

const stringToNumber = function(str){
  return +str
}