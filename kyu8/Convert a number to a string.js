function numberToString(num) {
    return num.toString()
  }

  const numberToString = num => String(num)

  const numberToString = num => String(num)

  const numberToString = num => String(num)

  function numberToString(num) {
    return num.toString()
  }

  function numberToString(num) {
    return num.toString()
  }

  function numberToString(num) {
    return num.toString()
  }