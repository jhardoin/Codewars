const main = (verb, noun) => verb + noun

const main = (verb, noun) => verb + noun

function main (verb, noun) {
    return verb + noun
  }

  function main(verb, noun) {
    return verb + noun
  }

  function main(verb, noun) {
    return verb + noun
  }