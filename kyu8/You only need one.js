function check(a, x) {
    return a.indexOf(x) > -1;
  }

  const check = (a, x) => a.includes(x)

  const check = (a, x) => a.includes(x)

  const check = (a, x) => a.includes(x)

  function check(a, x) {
    return a.includes(x)
  }

  function check(a, x) {
    return a.some(e => e == x)
  }

  function check(a, x) {
    return a.some(e => e == x)
  }