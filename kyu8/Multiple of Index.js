const multipleOfIndex = (array) => array.filter((element, index) => element % index == 0 || element === 0)

function multipleOfIndex(array) {
    return array.filter((e,i) => e % i === 0 || e == 0)
  }

  function multipleOfIndex(array) {
    return array.filter((e,i) => e % i === 0 || e == 0)
  }

  function multipleOfIndex(array) {
    return array.filter((e,i) => e % i === 0 || e == 0)
  }