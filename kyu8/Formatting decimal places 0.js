function twoDecimalPlaces(n) {
    return Number(n.toFixed(2))
  }

  function twoDecimalPlaces(n) {
    return +n.toFixed(2)
  }

  function twoDecimalPlaces(n) {
    return +n.toFixed(2)
  }

  function twoDecimalPlaces(n) {
    return +n.toFixed(2)
  }

  function twoDecimalPlaces(n) {
    return +n.toFixed(2)
  }