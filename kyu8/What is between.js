function between(a, b) {
    let newArr = [];
    for(let i = a; i <= b; i++){
      newArr.push(i)
    }
    
    return newArr
  }

  function between(a, b) {
    let solution = []
    for(let i = a; i <= b; i++){
      solution.push(i)
    }
    return solution
  }