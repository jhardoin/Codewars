function anyArrows(arrows){
    return arrows.some(e => !e.damaged)
  }

  function anyArrows(arrows){
    return arrows.some(e => !e.damaged)
  }

  function anyArrows(arrows){
    return arrows.some(e => !e.damaged)
  }