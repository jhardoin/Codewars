const maps = x => x.map(e => e * 2)

const maps = x => x.map(e => e * 2)

const maps = x => x.map(e => e * 2)

function maps(x){
    return x.map(e => e * 2)
  }

  function maps(x){
    return x.map(e => e * 2)
  }

  function maps(x){
    return x.map(e => e * 2)
  }

  function maps(x){
    return x.map(e => e * 2)
  }

  function maps(x){
    return x.map(e => e * 2)
  }