function combineNames (first, last) {
    return first + ' ' + last
  }

  function combineNames(first,last){
    return `${first} ${last}`
  }

  function combineNames(first,last){
    return `${first} ${last}`
  }

  function combineNames(first,last){
    return `${first} ${last}`
  }

  function combineNames(first,last){
    return `${first} ${last}`
  }