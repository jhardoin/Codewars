function repeatStr (n, s) {
    return s.repeat(n);
  }

  const repeatStr = (n, s) => s.repeat(n)

  const repeatStr = (n, s) => s.repeat(n)

  const repeatStr = (n, s) => s.repeat(n)

  function repeatStr (n, s) {
    return s.repeat(n)
  }