const boolToWord = bool => bool == true ? 'Yes' : 'No'

const boolToWord = bool => bool ? 'Yes' : 'No'

const boolToWord = bool => bool ? 'Yes' : 'No'

function boolToWord( bool ){
    return bool ? 'Yes' : 'No'
  }

  function boolToWord( bool ){
    return bool ? 'Yes' : 'No'
  }
