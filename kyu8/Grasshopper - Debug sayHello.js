function sayHello(name) {
    return `Hello, ${name}`
  }

  const sayHello = name => `Hello, ${name}`

  const sayHello = name => `Hello, ${name}`

  function sayHello(name) {
    return `Hello, ${name}`
  }

  function sayHello(name) {
    return `Hello, ${name}`
  }