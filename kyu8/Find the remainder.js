const remainder = (n, m) => (n > m) ? n % m : m % n

function remainder(n, m){
    return n > m ? n % m : m % n
  }

  function remainder(n, m){
    return Math.max(n,m) % Math.min(n,m)
  }

  function remainder(n, m){
    return Math.max(n,m) % Math.min(n,m)
  }

  function remainder(n, m){
    return Math.max(n,m) % Math.min(n,m)
  }