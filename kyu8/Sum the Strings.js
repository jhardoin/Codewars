function sumStr(a,b) {
    return (Number(a) + Number(b)).toString()
  }

  const sumStr = (a,b) => String(+a + +b)

  const sumStr = (a,b) => String(+a + +b)

  function sumStr(a,b) {
    return String(+a + +b)
  }

  function sumStr(a,b) {
    return String(+a + +b)
  }

  function sumStr(a,b) {
    return String(+a + +b)
}

function sumStr(a,b) {
  return String(+a + +b)
}