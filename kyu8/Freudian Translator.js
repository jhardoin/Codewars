function toFreud(string) {
    return string.length == 0 ? '' : string.split(' ').map(e => 'sex').join(' ')
    }

    function toFreud(string) {
        return string.length == 0 ? '' : string.split(' ').map(e => 'sex').join(' ')
        }